"""
Package 'test_routers'.

Package components:
1. The 'test_images' folder contains test files for testing the media route.
2. The 'upload_test_images' folder is intended for temporary storage of files
that passed validation during testing.
3. The 'test_media' module tests the media route.
4. The 'test_tweets' module tests the tweets route.
5. The 'test_users' module tests the users route.
"""
